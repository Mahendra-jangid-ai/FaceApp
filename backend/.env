# Server
PORT=8080
ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=yourpassword
DB_NAME=faceapp

# JWT
JWT_SECRET=change-me-in-production-use-strong-secret
